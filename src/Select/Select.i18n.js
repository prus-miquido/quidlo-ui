import {defineMessages} from 'react-intl';

export default defineMessages({
    all: {
        id: 'select.error.all',
        defaultMessage: 'All'
    },
    noOptions: {
        id: 'select.noOptions',
        defaultMessage: 'No options'
    },
});